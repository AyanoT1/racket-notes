#lang play

; Resumen de la clase: Teniendo la sustitución, se pueden implementar las funciones de primer order.
; en la clase se extiende el parser, luego la sustitución, se agrega la lista de fundefs como arg
; opcional para no romper los tests anteriores y finaliza con que el lenguaje puede definir
; recursiones infinitas porque no tiene un if.